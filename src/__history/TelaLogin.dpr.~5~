program TelaLogin;

uses
  System.StartUpCopy,
  FMX.Forms,
  view.principal in 'views\view.principal.pas' {Form1},
  interfaces.usuario in 'interfaces\interfaces.usuario.pas',
  classe.usuario in 'classes\classe.usuario.pas',
  interfaces.conexao.firedac in 'interfaces\interfaces.conexao.firedac.pas',
  classe.conexao.firedac in 'classes\classe.conexao.firedac.pas',
  classe.query.firedac in 'classes\classe.query.firedac.pas',
  controller.factory.query in 'controllers\controller.factory.query.pas',
  interfaces.factoryquery in 'interfaces\interfaces.factoryquery.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
